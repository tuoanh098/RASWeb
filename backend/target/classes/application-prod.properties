# application-prod.properties
server.port=${SERVER_PORT:8080}

# DB via environment variables
spring.datasource.url=jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:ras_music}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh&characterEncoding=utf8
spring.datasource.username=tuoanh
spring.datasource.password=Tuoanh098186@

# JPA for production
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# CORS for production domain
spring.mvc.cors.mappings.[/api/**].allowed-origins=${ALLOWED_ORIGIN:https://rasmusic.icu}
spring.mvc.cors.mappings.[/api/**].allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
spring.mvc.cors.mappings.[/api/**].allowed-headers=*
spring.mvc.cors.mappings.[/api/**].allow-credentials=true

# App-specific
app.upload.dir=uploads
app.public-base-url=${PUBLIC_BASE_URL:https://rasmusic.icu}
